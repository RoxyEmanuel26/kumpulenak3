/**
 * lib/db/prisma.ts
 *
 * DEPRECATED: Prisma client removed from edge runtime to reduce bundle size.
 * All database queries now use @neondatabase/serverless (neon HTTP client) directly.
 *
 * For schema management and migrations, use: npx prisma migrate dev
 */

export {};
